import React, { Component } from 'react';
import {
    useTable,
    useGroupBy,
    useFilters,
    useSortBy,
    useExpanded,
    usePagination
  } from 'react-table'

export default class table extends Component {
    render() {
        return (
            <div>
                
            </div>
        )
    }
}
